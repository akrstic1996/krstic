import './App.css';
import Nav from './Nav';

function App() {
  // Root is html doc
  // We can make the Header a flexbox, with the navbar nested flexbox inside the header
  // Header consists of Logo title and nav
  return (
        <Nav />
  );
}

export default App;
